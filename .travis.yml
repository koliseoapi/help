sudo: false
language: node_js
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y hugo
script: npm run test
after_script: git add docs/* && git commit -m 'Automatic deployment from Travis' && git push origin master
node_js:
  - "stable"
  